@model IEnumerable<lab_65_Football_API.Models.Owner>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OwnerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NetWorth)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OwnerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NetWorth)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.OwnerID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.OwnerID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.OwnerID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
